#include <iostream>
#include <vector>

using namespace std;

//Global Variables
int client_num = 0, truck_num = 0, max_cap = 0;

vector <int> city;
vector <bool> visited


//
int main(int argc, char **argv){
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    //INPUT
    cin >> client_num >> truck_num >> max_cap;

    city.resize(client_num + 1);
    visited.resize(client_num + 1);

    //


    //OUTPUT


    return 0;
}